# 클라우드 MLOps 교육계획서 (v3)

**연성대학교 · 주 1회 14주 · 회차당 230분 운영**
작성일: 2026-08-02 · v3 (NCS 5개 세분류 매핑 반영)

---

## 1. 교과목 개요

| 항목 | 내용 |
|---|---|
| 교과목명 | 클라우드 MLOps |
| 선수학습 | Python 기초 + 머신러닝 이론 수강 완료 |
| 운영 | 주 1회 4시간 편성 · **실제 운영 230분(10분 조기 종료)** × 14주 |
| 클라우드 | AWS — 학과 결제 계정 기반 **학생별 IAM 계정** |
| 고정 리전 | `ap-northeast-2` (서울) — 전 학기 고정, 변경 금지 |
| 실습 기조 | **하이브리드**: EC2 + Docker + FastAPI로 직접 구축(핵심) + SageMaker/Bedrock 체험(2회) |
| 과제 | 팀 프로젝트 1건 (3~4인, 총 공수 18~20시간) |

### 한 줄 요약

> "노트북 안에서 끝나는 모델"을 **주소를 가진 서비스**로 만들어보는 과목.
> 주제 선정 → 데이터 → 모델 학습 → 컨테이너 → API 서빙 → 자동 배포 → 모니터링까지
> **한 줄기 파이프라인을 학생이 직접 관통**한다.

---

## 2. 설계 원칙 (난이도 관리)

기초가 약한 학생도 낙오하지 않도록 다음 원칙을 전 회차에 적용한다.

1. **레퍼런스 구현 우선**
   강사가 공통 예제 데이터로 A~Z 골격을 먼저 완성해 보이고, 팀은 **같은 골격에 자기 데이터만 갈아 끼운다.**
   → 학생이 "설계"할 것은 주제와 데이터뿐. 구조는 그대로 복제.

2. **매 회차 "돌아가는 결과물" 하나**
   개념만 배우고 끝나는 회차를 만들지 않는다. 각 회차 끝에 눈에 보이는 산출물(스크린샷/URL/파일)이 남는다.

3. **복붙 가능한 자산 제공**
   - 회차별 명령어 치트시트 (1페이지)
   - 실행 가능한 스크립트 / Dockerfile / YAML 템플릿 (빈칸 채우기 방식)
   - 회차별 **완성본 저장소 태그** (`week-05-done` 등) → 뒤처진 학생은 완성본에서 이어감

4. **이론:실습 = 3:7**
   이론은 "왜 필요한가"만 짧게. 원리 설명은 실습 중 문제가 터졌을 때 붙인다.

5. **실패 복구 절차 표준화**
   "인스턴스 접속 안 될 때", "도커 빌드 실패할 때" 등 상위 5개 장애의 복구 절차를 사전 배포.

6. **비용은 수업의 일부**
   매 수업 종료 5분 전 **리소스 정리 타임**을 고정 운영.

7. **실습 시간의 절반 이상은 직접 손으로**
   따라치기(실습 A)보다 **스스로 만드는 시간(실습 B)을 길게** 잡는다. 실습 A는 "어떻게 하는지 한 번 보는" 용도이고, 손에 남는 것은 실습 B에서 만들어진다.

### 표준 시간표 (230분 · **10분 조기 종료**)

| 시간 | 구성 | 분 |
|---|---|---|
| 00:00–00:10 | 지난주 복습 + **오늘 만들 결과물 데모** | 10 |
| 00:10–00:55 | 이론 강의 | 45 |
| 00:55–01:05 | 휴식 | 10 |
| 01:05–01:55 | **실습 A** — 강사 따라하기 (공통 예제) | 50 |
| 01:55–02:05 | 휴식 | 10 |
| 02:05–03:30 | **실습 B** — 각자/팀이 직접 수행 | **85** |
| 03:30–03:45 | 체크포인트 제출 + 다음주 예고 | 15 |
| 03:45–03:50 | **리소스 정리 타임** (인스턴스 중지 확인) | 5 |

> **v1 대비 변경**: 총 240분 → **230분**(10분 조기 종료). 공통 따라하기(실습 A) 60→50분으로 축소하고, **직접 수행하는 실습 B를 70→85분으로 확대**. 이론·도입·정리에서 각 5분씩 덜어냄.
>
> 실습 A를 50분 안에 끝내기 위한 장치:
> - 실습 A에서 타이핑 분량이 많은 부분(설정 파일, 보일러플레이트)은 **사전 배포 파일**로 대체하고 핵심 라인만 직접 입력
> - 실습 A 종료 시점을 강제 컷오프. 못 따라온 학생은 완성본 태그(`week-NN-done`)에서 이어받아 실습 B로 진입
> - 강사 시연은 "설명하며 치기"가 아니라 **미리 친 코드를 읽어주고 학생이 치게 하기**

---

## 3. 학습 목표 (CLO)

| # | 학습 목표 | 관련 회차 |
|---|---|---|
| CLO1 | AWS 기본 리소스(IAM·S3·EC2)를 콘솔과 CLI로 다룰 수 있다 | 1–2 |
| CLO2 | 클라우드 저장소 기반의 재현 가능한 데이터·학습 코드를 작성할 수 있다 | 3–4 |
| CLO3 | 실험 기록 도구로 모델을 비교·선택하고 근거를 설명할 수 있다 | 5 |
| CLO4 | 모델을 컨테이너로 패키징하고 REST API로 서빙할 수 있다 | 6–8 |
| CLO5 | 관리형 ML/생성형 AI 서비스를 직접 구축 방식과 비교·활용할 수 있다 | 9–10 |
| CLO6 | 배포 자동화와 모니터링의 기본 흐름을 구성할 수 있다 | 11–12 |
| CLO7 | 팀으로 문제 정의부터 서비스 시연까지 수행하고 발표할 수 있다 | 3–14 |

---

## 4. NCS 연계 (국가직무능력표준)

본 교과목은 아래 **5개 세분류**에 걸쳐 매핑됩니다. 회차별 능력단위요소는 9장 상세 계획에 병기했습니다.

| 세분류 | 코드 | 전체 요소 | 채택 | 비율 | 역할 |
|---|---|---|---|---|---|
| 인공지능모델링 | `20010703` | 29 | 19 | 66% | **주** — 데이터·모델 라인 (3~5주) |
| 인공지능서비스운영관리 | `20010704` | 29 | 13 | 45% | **주** — 운영 라인 (11~13주) |
| 인공지능플랫폼구축 | `20010701` | 34 | 11 | 32% | 연계 — 구현·서빙 (2·6~10주) |
| 인공지능서비스기획 | `20010702` | 30 | 5 | 17% | 연계 — 기획·성과평가 (1·3·14주) |
| 빅데이터플랫폼구축 | `20010209` | 26 | 2 | 8% | 연계 — 데이터 수집·정의 (3주) |
| **합계** | | **148** | **50** | **34%** | |

**주 세분류** `20010703 인공지능모델링` (데이터·모델 라인) · `20010704 인공지능서비스운영관리` (운영 라인)
**연계 세분류** `20010701 인공지능플랫폼구축` · `20010702 인공지능서비스기획` · `20010209 빅데이터플랫폼구축`

### 매핑 원칙
1. **억지 매핑을 하지 않는다.** 교과목 진행 순서를 유지하고 정합하는 요소만 연결한다.
2. 채택률(34%)이 낮은 것은 의도된 결과다. 5개 세분류는 각각 조직 단위 직무를 상정하므로 전량 소화 대상이 아니다.
3. 제외한 영역은 **명시적으로 밝힌다** (아래).

### 의도적 미채택 영역

| 세분류 | 제외 대상 | 사유 |
|---|---|---|
| 20010209 | `2001020905` 빅데이터 처리시스템 개발 | Hadoop/Spark/Kafka 분산·실시간 처리 — 기능사 수준 난이도와 충돌, 서빙 라인 잠식 |
| 20010704 | `2001070402` 이용자관리(과금·VOC), `2001070407` 운영수준관리(SLA) | 조직 업무·협의 대상 부재. 12주차 이론에서 개념만 소개 |
| 20010701 | `2001070102` 요구사항 분석, `2001070103` 설계, `2001070107` 지식화 구현 | 플랫폼을 짓는 직무. 본 과목은 플랫폼 위에서 모델을 운영 |
| 20010702 | `2001070204`~`2001070207` 서비스 기획 계열 | 기획 직무 비중이 커 실습형 4시간 수업과 부정합 |

> 상세 매핑 근거와 v1 대비 개선 내역은 별도 문서 `NCS매핑표_v2.md` 참조.

---

## 5. 도구 스택

| 구분 | 도구 | 비고 |
|---|---|---|
| 언어/라이브러리 | Python 3.11, pandas, scikit-learn | 딥러닝 프레임워크는 선택 사항 |
| 실험 관리 | MLflow | EC2 자체 호스팅 (SQLite + S3) |
| 서빙 | FastAPI + Uvicorn, Streamlit(데모 UI) | |
| 컨테이너 | Docker, Docker Compose, Amazon ECR | |
| AWS | IAM, S3, EC2, ECR, CloudWatch, Budgets, SageMaker AI, Bedrock | |
| 자동화 | Git, GitHub, GitHub Actions | YAML 템플릿 제공 |
| 협업 | GitHub 팀 저장소, 공유 문서 | |

> **참고**: SageMaker는 2025년 이후 `Amazon SageMaker AI`(기존 ML 기능)와 `SageMaker Unified Studio`(통합 콘솔)로 명칭이 분리되었다. 본 과목은 **SageMaker AI Studio의 노트북 · 학습 잡 · 엔드포인트**만 사용한다. 개강 전 콘솔 화면 캡처를 갱신할 것.

---

## 6. AWS 계정 · 비용 운영 정책

### 계정 구성
- 학과 결제 계정 하위에 **학생별 IAM 사용자** 발급 (`mlops-2026-학번`)
- 팀별 리소스 태그 필수: `Course=MLOps`, `Team=T01`, `Owner=학번`
- IAM 권한 경계: 허용 서비스(S3/EC2/ECR/CloudWatch/SageMaker/Bedrock) + **인스턴스 타입 화이트리스트**(`t3.micro/small/medium`)만 허용, GPU 인스턴스 차단

### 비용 안전장치
1. AWS Budgets — **학생별 월 한도 $10 + 강사 전체 한도** 설정, 50/80/100% 이메일 알람
2. 매 수업 종료 전 5분 리소스 정리 타임 (인스턴스 Stop 확인 → 조교 체크)
3. SageMaker 엔드포인트 · Studio 스페이스는 **당일 삭제 원칙** (9주차 체크리스트에 명시)
4. 주 1회 강사가 Cost Explorer에서 태그별 사용량 확인

### 예상 비용 (학생 1인, 학기 전체 · 개략치)

| 항목 | 추정 |
|---|---|
| EC2 t3.medium (수업 56h + 자율 40h) | 약 $5 |
| EBS 30GB × 4개월 | 약 $12 |
| S3 / ECR / CloudWatch | 약 $2 |
| SageMaker 학습 잡 + 엔드포인트 (당일 삭제 기준) | 약 $5 |
| Bedrock 텍스트 생성 (소량) | 약 $1 |
| **합계** | **약 $25 내외** |

> 30명 기준 학기 총액 대략 $750~900. 정리 타임을 지키지 않으면 EBS·엔드포인트 비용이 배로 늘어나므로, **비용 관리를 평가 항목에 포함**한다.

---

## 7. 평가 방법 (100점)

| 항목 | 배점 | 설명 |
|---|---|---|
| 출석 및 참여 | 10 | |
| 주차별 실습 체크포인트 | 25 | 12개 회차 × 약 2점, 스크린샷/URL 제출 |
| M1 문제정의서 | 10 | 4주차 제출 |
| M2 중간 시연 | 15 | 8주차, 팀별 5분 |
| 최종 산출물 (코드·문서·배포) | 25 | 루브릭 하단 참조 |
| 최종 발표 + 상호평가 | 15 | 14주차 |

### 최종 산출물 루브릭 (25점)

| 항목 | 배점 | 상(우수) | 하(미흡) |
|---|---|---|---|
| 파이프라인 완성도 | 10 | 데이터→학습→서빙 전 구간이 실제로 동작하고 시연 가능 | 학습만 되고 API 미동작 |
| 실험 관리·재현성 | 5 | MLflow에 비교 실험 5건 이상, README만 보고 재현 가능 | 기록 없음 |
| 배포·운영 | 5 | 컨테이너 배포 + 로그/알람 구성 | 로컬 실행만 |
| 문서화·발표 | 5 | 문제정의·의사결정 근거가 명확 | 코드 나열 수준 |

> **NCS 연계**: 주차별 실습 체크포인트(25점)와 최종 산출물 루브릭(25점)의 평가 문항은
> 해당 회차에 매핑된 능력단위요소의 **수행준거**를 그대로 문항화해 사용한다 (`ncs_수행준거_전체.json`).

### 감점 요소
- 리소스 미정리(엔드포인트 방치 등) 적발 시 회당 -2점
- 개인정보·저작권 위반 데이터 사용 시 프로젝트 점수 0점

---

## 8. 주차별 교육 계획 (요약표)

| 주차 | 주제 | 핵심 실습 | 산출물 | 마일스톤 |
|---|---|---|---|---|
| 1 | MLOps 개요 & 클라우드 첫걸음 | 콘솔 로그인, IAM, S3, CloudShell CLI | 개인 S3 버킷 | |
| 2 | 리눅스 · EC2 개발환경 구축 | EC2 생성·SSH·Python 환경·boto3 | 접속 가능한 개발 서버 | |
| 3 | 데이터 파이프라인 & 문제 정의 | S3 적재 → pandas 전처리 스크립트 | 전처리 스크립트, 문제정의서 초안 | 프로젝트 착수 |
| 4 | 베이스라인 모델 만들기 | scikit-learn 학습 → 모델 저장 → S3 | `model.pkl` + 성능 리포트 | **M1 제출** |
| 5 | 실험 관리 (MLflow) | MLflow 서버 구축, 실험 5건 비교 | 실험 비교표, 등록 모델 | |
| 6 | 컨테이너 기초 (Docker · ECR) | Dockerfile 작성, 빌드, ECR push | ECR 이미지 | |
| 7 | 모델 서빙 API (FastAPI) | `/health` `/predict` 구현 + 컨테이너화 | 동작하는 예측 API | |
| 8 | 배포와 데모 화면 · 중간 점검 | EC2 배포, Streamlit 데모 UI | 외부 접속 가능한 데모 | **M2 중간 시연** |
| 9 | 관리형 ML 서비스 (SageMaker AI) | 학습 잡 + 엔드포인트 배포/삭제 | 직접구축 vs 관리형 비교표 | |
| 10 | 생성형 AI 활용 (Bedrock) | Bedrock 호출, 프로젝트 보조 기능 | LLM 기능 엔드포인트 1개 | |
| 11 | 배포 자동화 (GitHub Actions) | push → 빌드 → ECR → 재배포 | 자동 배포 파이프라인 | |
| 12 | 모니터링과 운영 | CloudWatch 로그·메트릭·알람 | 대시보드 + 알람 | **M3 파이프라인 완성** |
| 13 | 프로젝트 집중 워크숍 | 통합·문서화·리허설, 팀별 코칭 | 발표자료, 시연 영상 백업 | |
| 14 | 최종 발표 및 마무리 | 발표·상호평가·리소스 전체 정리 | 최종 산출물 | **M4 최종** |

---

## 9. 주차별 상세 계획

### 1주차 — MLOps 개요 & 클라우드 첫걸음

**학습목표** 이 과목에서 최종적으로 무엇을 만드는지 그림으로 이해하고, AWS 콘솔에 로그인해 S3에 파일을 올릴 수 있다.

- **이론(45분)**
  - 모델이 서비스가 되기까지: "정확도 92% 모델"이 왜 아무도 못 쓰는 상태로 남는가
  - MLOps 구성요소 지도 = **이 과목 14주 로드맵** (한 장 그림으로 제시, 매주 이 그림에 위치 표시)
  - 클라우드 기본 개념: 리전 / 가용영역 / 온디맨드 / 책임 공유 모델
  - **최종 결과물 데모**: 작년 학생 프로젝트 또는 강사 샘플 서비스 시연 (동기 부여)
- **실습 A(50분)** 콘솔 로그인 → MFA 설정 → 리전을 서울로 고정 → IAM 사용자/권한 확인 → Budgets 알람 확인
- **실습 B(85분)** S3 버킷 생성 → 폴더 규칙(`raw/`, `processed/`, `models/`) 생성 → 파일 업로드/다운로드 → **CloudShell에서 `aws s3 cp/ls`로 같은 작업 반복**
- **체크포인트** 본인 버킷 이름 + CLI 실행 결과 스크린샷 제출
- **과제** 팀 구성 희망서(3~4인) 제출, 관심 도메인 1순위 표기
- **강사 사전 준비** IAM 계정 일괄 발급, Budgets 설정, Bedrock 모델 액세스 사전 승인 요청
- **NCS 능력단위요소** `2001070101_18v1.1` 인공지능 플랫폼 동향 분석하기 (부분) / `2001070201_18v1.3` 인공지능 서비스 기술 환경 분석하기 (부분) / `2001070101_18v1.5` 인공지능 플랫폼 구축 비용 계획하기 (부분)

---

### 2주차 — 리눅스 · EC2 개발환경 구축

**학습목표** EC2 인스턴스를 직접 만들고 접속해서 Python 실습 환경을 세팅할 수 있다.

- **이론(45분)**
  - 서버란 무엇인가 / EC2 구성요소: AMI · 인스턴스 타입 · EBS · 키페어 · 보안그룹
  - 보안그룹 = 방화벽. 22번, 8000번 포트의 의미
  - 리눅스 최소 생존 명령 15개 (`ls cd pwd mkdir cp mv rm cat less tail chmod ps kill df top`)
- **실습 A(50분)** EC2(Ubuntu, t3.medium) 생성 → 키페어 다운로드 → SSH 접속 → 리눅스 명령 연습 → `apt` 패키지 설치
- **실습 B(85분)** Python 가상환경 생성 → JupyterLab 설치 및 원격 접속(또는 VS Code Remote SSH) → `boto3`로 1주차 S3 데이터 읽기
- **체크포인트** 노트북에서 S3 파일을 DataFrame으로 읽은 화면
- **과제** 인스턴스 중지 → 재시작 → 재접속 연습 (IP 바뀌는 것 체험), 명령어 정리 노트
- **난이도 대응** 사내망/학교망 SSH 차단 시 → **EC2 Instance Connect** 또는 **Session Manager**로 대체 (사전 테스트 필수)
- **NCS 능력단위요소** `2001070104_18v1.1` 인공지능 플랫폼 하드웨어 환경 구현하기 / `2001070104_18v1.2` 인공지능 플랫폼 소프트웨어 환경 구현하기

---

### 3주차 — 데이터 파이프라인 기초 & 문제 정의

**학습목표** S3의 원본 데이터를 전처리해 다시 S3에 저장하는 스크립트를 만들고, 팀 주제를 ML 문제로 정의할 수 있다.

- **이론(45분)**
  - "모델보다 데이터" — 실무에서 성능을 가르는 것
  - 데이터 저장 규칙: `raw/` (절대 수정 금지) → `processed/` → `features/`
  - **문제를 ML 문제로 바꾸기**: 입력은 무엇인가 / 출력은 무엇인가 / 무엇으로 잘했다고 판단하나
  - 데이터 카드(출처·기간·행수·라이선스·개인정보 여부) 작성법
- **실습 A(50분)** 공통 예제 데이터(따릉이 대여 이력 등)를 S3에 적재 → pandas 결측·이상치 처리 → 파생변수 생성 → 결과를 `processed/`에 저장하는 **스크립트 1개** 완성
- **실습 B(85분)** 팀 편성 확정 → 도메인 선택 → 데이터 후보 탐색(공공데이터포털 · Kaggle) → **문제정의서 초안** 작성
- **체크포인트** 전처리 스크립트 + 데이터 카드
- **🏁 프로젝트 착수 (누적 공수 ~1.5h)**
- **NCS 능력단위요소** `2001070301_19v1.1` 인공지능 모델 목표 설정하기 / `2001070301_19v1.2` 인공지능 모델 요구사항 정의하기 / `2001070303_19v1.3` 인공지능 내·외부 데이터 수집하기 / `2001070304_19v1.1` 인공지능 데이터 정제하기 / `2001070304_19v1.3` 인공지능 데이터 변환하기 / `2001070202_18v1.2` 인공지능 서비스 목표항목 정의하기 (부분) / `2001020903_24v3.3` 빅데이터 수집·변환 모듈 개발하기 (부분) / `2001020901_24v3.2` 빅데이터 플랫폼 요구사항 정의하기 (부분)

---

### 4주차 — 베이스라인 모델 만들기

**학습목표** scikit-learn으로 베이스라인 모델을 학습·평가·저장하고 S3에 업로드할 수 있다.

- **이론(45분)**
  - 학습/검증/테스트 분할, 데이터 누수(leakage)
  - 평가지표 고르기: 정확도 · F1 · RMSE · MAE — **문제에 따라 다르다**
  - 베이스라인의 의미: "이보다 못하면 모델을 쓸 이유가 없다"의 기준선
- **실습 A(50분)** `Pipeline`으로 전처리+모델 묶기 → 학습 → 평가 → `joblib` 저장 → S3 `models/` 업로드
- **실습 B(85분)** 팀 데이터로 베이스라인 시도, 성능 기록
- **체크포인트** `model.pkl` S3 경로 + 성능 수치
- **🏁 M1 제출** 문제정의서 최종본 (주제 · 데이터 · 입출력 · 평가지표 · 성공 기준). 다음 주 시작 시 팀별 3분 발표
- **누적 공수 ~3h**
- **NCS 능력단위요소** `2001070301_19v1.3` 인공지능 후보 모델 도출하기 / `2001070302_19v1.1` 인공지능 모델 기본 설계하기 / `2001070305_19v1.2` 인공지능 특징 생성하기 (부분) / `2001070306_19v1.1` 인공지능 학습 알고리즘 선정하기 / `2001070306_19v1.4` 인공지능 학습하기 / `2001070307_19v1.1` 인공지능 모델 평가 기준 정하기

---

### 5주차 — 실험 관리 (MLflow)

**학습목표** 실험 기록 도구로 여러 모델을 비교하고, 선택 근거를 남길 수 있다.

- **이론(45분)**
  - "3주 전 그 결과 어떻게 냈지?" — 재현성 문제
  - 실험 추적의 4요소: 파라미터 · 메트릭 · 아티팩트 · 코드 버전
  - 모델 레지스트리와 스테이지(Staging/Production) 개념
- **실습 A(50분)** EC2에 MLflow 서버 기동(SQLite 백엔드 + S3 아티팩트) → 학습 코드에 `log_param`/`log_metric`/`log_model` 삽입 → **하이퍼파라미터 5조합 실행 후 UI에서 비교**
- **실습 B(85분)** 팀 프로젝트에 MLflow 적용 → 최고 성능 모델을 레지스트리에 등록
- **체크포인트** 실험 비교 화면 스크린샷 + 등록 모델명
- **누적 공수 ~4.5h**
- **NCS 능력단위요소** `2001070306_19v1.3` 인공지능 인자 조율하기 / `2001070307_19v1.2` 인공지능 모델 선정 기준 정하기 / `2001070307_19v1.3` 인공지능 학습 결과 검증하기 / `2001070307_19v1.4` 인공지능 최적화 모델 선정하기 / `2001070308_19v1.1` 인공지능 선정모델 구성 요소 관리하기

---

### 6주차 — 컨테이너 기초 (Docker · ECR)

**학습목표** Dockerfile을 작성해 이미지를 빌드하고 ECR에 올릴 수 있다.

- **이론(45분)**
  - "제 컴퓨터에선 되는데요" 문제 → 컨테이너가 푸는 것
  - 이미지 vs 컨테이너, 레이어, `FROM/COPY/RUN/CMD`
  - 레지스트리 개념과 ECR
- **실습 A(50분)** EC2에 Docker 설치 → `hello-world` → 파이썬 스크립트를 컨테이너로 실행 → Dockerfile 직접 작성 → 빌드/실행 → `requirements.txt` 관리
- **실습 B(85분)** ECR 리포지토리 생성 → 로그인 → `tag` → `push` → 삭제 후 `pull`로 복구 확인
- **체크포인트** ECR 콘솔의 본인 이미지 화면
- **난이도 대응** Dockerfile은 **빈칸 채우기 템플릿** 제공. 처음부터 백지 작성 요구하지 않음
- **NCS 능력단위요소** `2001070104_18v1.2` 인공지능 플랫폼 소프트웨어 환경 구현하기 / `2001070308_19v1.1` 인공지능 선정모델 구성 요소 관리하기 (부분)

---

### 7주차 — 모델 서빙 API (FastAPI)

**학습목표** 학습한 모델을 HTTP로 호출 가능한 예측 API로 만들 수 있다.

- **이론(45분)**
  - 배치 추론 vs 실시간 추론 — 우리 프로젝트는 어느 쪽인가
  - REST API 기초, 요청/응답 JSON 스키마
  - **학습-서빙 스큐**: 학습 때 쓴 전처리를 서빙에서 똑같이 해야 하는 이유 (Pipeline 통째로 저장하는 이유)
- **실습 A(50분)** FastAPI로 `/health`, `/predict` 구현 → 모델 로드 → Swagger UI로 테스트 → Pydantic 입력 검증
- **실습 B(85분)** 모델 + API를 하나의 Docker 이미지로 빌드 → 컨테이너 실행 → 다른 터미널에서 `curl` 호출 → ECR push
- **체크포인트** `curl` 요청/응답 캡처
- **누적 공수 ~6h**
- **NCS 능력단위요소** `2001070105_18v1.2` 인공지능 추론 기능 구현하기 / `2001070106_18v1.3` 인공지능 플랫폼 외부 인터페이스 구현하기 / `2001070108_18v1.1` 인공지능 플랫폼 단위 테스트하기

---

### 8주차 — 배포와 데모 화면 · 중간 점검

**학습목표** 컨테이너를 EC2에 배포해 외부에서 접속 가능한 데모를 만들 수 있다.

- **이론(35분)**
  - 보안그룹 · 포트 개방 · 퍼블릭 IP / 탄력적 IP
  - 비밀 정보 다루기: 코드에 키를 넣지 않는다 (환경변수 · IAM 역할)
  - 데모 화면의 가치 — 심사자는 API가 아니라 화면을 본다
- **실습 A(50분)** ECR 이미지를 EC2에서 `pull` → `docker compose`로 API + Streamlit UI 동시 실행 → 브라우저에서 예측 실행
- **실습 B(75분)** 팀 프로젝트 중간 통합 + 시연 준비
- **🏁 M2 중간 시연 (마지막 40분)** 팀별 5분: 문제 정의 / 베이스라인 성능 / **동작하는 API 호출 시연**
- *(시간 배분: 도입10 + 이론35 + 휴식20 + 실습A 50 + 실습B 75 + 시연40 = 230분)*
- **누적 공수 ~8.5h**
- **NCS 능력단위요소** `2001070308_19v1.2` 인공지능 선정모델 배포 관리하기 / `2001070106_18v1.1` 인공지능 플랫폼 휴먼머신 인터랙션 구현하기 / `2001070108_18v1.2` 인공지능 플랫폼 통합 테스트하기

---

### 9주차 — 관리형 ML 서비스 체험 (SageMaker AI)

**학습목표** 관리형 서비스로 학습·배포를 해보고, 직접 구축 방식과 장단점을 비교할 수 있다.

- **이론(45분)**
  - 관리형 서비스가 대신 해주는 것 / 대신 잃는 것
  - 직접 구축 vs 관리형: 비용 · 속도 · 자유도 · 운영 부담
- **실습 A(50분)** SageMaker AI Studio 노트북 실행 → 같은 데이터로 **학습 잡(Training Job)** 실행 → S3 산출물 확인
- **실습 B(85분)** 실시간 **엔드포인트 배포** → 호출 → **즉시 삭제** → 우리가 7~8주차에 만든 FastAPI 방식과 비교표 작성
- **체크포인트** 비교표(직접구축 vs SageMaker: 구축 시간·비용·수정 자유도·운영 부담)
- **⚠ 비용 경고** 이 회차는 종료 전 **엔드포인트·노트북 스페이스 삭제 확인**을 조교가 1:1 점검
- **NCS 능력단위요소** `2001070105_18v1.1` 인공지능 학습 기능 구현하기 / `2001070105_18v1.4` 인공지능 모델링 기능 구현하기 (부분) / `2001070101_18v1.5` 인공지능 플랫폼 구축 비용 계획하기 (부분)

---

### 10주차 — 생성형 AI 활용 (Amazon Bedrock)

**학습목표** Bedrock으로 파운데이션 모델을 호출하고, 프로젝트에 보조 기능으로 붙일 수 있다.

- **이론(45분)**
  - 파운데이션 모델 / 프롬프트 / 토큰과 과금 구조
  - **언제 LLM을 쓰고 언제 전통 ML을 쓰나** — 판단 기준 정리
  - 생성형 AI의 한계: 환각, 비결정성, 비용, 개인정보
- **실습 A(50분)** Bedrock 모델 액세스 확인 → `boto3`로 텍스트 생성 호출 → 프롬프트 변경 실험
- **실습 B(85분)** 프로젝트에 보조 기능 1개 붙이기
  - 예측형 팀: "예측 결과를 사람이 읽을 문장으로 설명해주는" 엔드포인트
  - 텍스트형 팀: 리뷰 요약 · 키워드 추출
- **체크포인트** LLM 기능이 붙은 API 응답 캡처
- **강사 사전 준비** 개강 전 Bedrock 모델 액세스 승인 완료 확인 (승인에 시간이 걸릴 수 있음), 서울 리전 사용 가능 모델 목록 갱신
- **누적 공수 ~10h**
- **NCS 능력단위요소** `2001070106_18v1.3` 인공지능 플랫폼 외부 인터페이스 구현하기 / `2001070206_18v1.1` 인공지능 서비스 활용 방안 분석하기 (부분)

---

### 11주차 — 배포 자동화 (Git · GitHub Actions)

**학습목표** 코드를 push하면 자동으로 이미지가 빌드되고 서버에 반영되는 파이프라인을 만들 수 있다.

- **이론(45분)**
  - 수동 배포의 문제 (누락 · 실수 · "누가 마지막에 뭘 올렸지")
  - CI/CD 개념, 파이프라인 단계: 테스트 → 빌드 → 푸시 → 배포
  - Git 최소 흐름: `clone` → `branch` → `commit` → `push` → PR
- **실습 A(50분)** 팀 GitHub 저장소 정리 → `.gitignore` → 브랜치·PR 1회 경험
- **실습 B(85분)** **제공된 워크플로 템플릿의 빈칸만 채워** 완성: push → 간단 테스트 → 이미지 빌드 → ECR push → EC2 재배포(SSM Run Command)
- **체크포인트** 커밋 1회로 배포된 로그
- **난이도 대응** YAML 전체 작성은 요구하지 않음. 리포지토리명·리전·인스턴스 ID 등 **값만 채우는 방식**. OIDC/시크릿 설정은 강사가 사전 구성
- **NCS 능력단위요소** `2001070308_19v1.2` 인공지능 선정모델 배포 관리하기 / `2001070401_20v1.2` 인공지능서비스 운영절차 수립하기 (부분) / `2001070406_20v1.3` 인공지능서비스 운영장애 해결하기 (부분)

---

### 12주차 — 모니터링과 운영

**학습목표** 서비스 로그와 지표를 수집하고 이상 시 알람을 받을 수 있다.

- **이론(45분)**
  - 서비스가 죽는 이유 top 5, 로그 · 메트릭 · 알람의 차이
  - **모델은 시간이 지나면 나빠진다** — 데이터 드리프트와 재학습 주기
  - 운영 체크리스트라는 문화
- **실습 A(50분)** CloudWatch Agent로 API 로그 수집 → 커스텀 메트릭(추론 횟수 · 응답시간 · 에러율) → 대시보드 구성 → 에러율 알람 + 이메일 구독
- **실습 B(85분)** 입력 데이터 분포 간이 모니터링 스크립트(학습 데이터 평균 대비 최근 입력 비교) + 팀 운영 체크리스트 작성
- **체크포인트** 대시보드 캡처 + 알람 수신 메일
- **🏁 M3** 데이터 → 학습 → 서빙 → 배포 → 모니터링 **전 구간 연결 완료**
- **누적 공수 ~11.5h**
- **NCS 능력단위요소** `2001070404_20v1.1` 인공지능서비스 운영 자원 점검하기 / `2001070404_20v1.2` 인공지능서비스 운영 인터페이스 점검하기 / `2001070404_20v1.3` 인공지능서비스 운영 성능 모니터링하기 / `2001070404_20v1.4` 인공지능서비스 운영모니터링 결과 관리하기 / `2001070405_20v1.1` 인공지능서비스 운영품질지표 구성하기 / `2001070308_19v1.3` 인공지능 선정모델 성능 관리하기 / `2001070406_20v1.1` 인공지능서비스 운영장애 접수하기 (부분)

---

### 13주차 — 프로젝트 집중 워크숍

**학습목표** 산출물을 통합·문서화하고 발표를 리허설한다.

- **미니 강의(20분)** 발표 구성법(문제 → 접근 → 결과 → 배운 점), **데모 실패 대비: 시연 영상 백업 필수**
- **팀별 코칭 로테이션(175분)** 강사가 팀당 15~20분 순회. 점검 항목:
  - 전 구간이 실제로 도는가 (다른 사람 계정에서 호출되는가)
  - README만 보고 재현 가능한가
  - MLflow 실험 기록이 남아 있는가
  - 리소스·비용 정리 상태
- **산출물** 발표자료 초안 · README · **시연 영상 백업 파일**
- *(시간 배분: 미니강의20 + 휴식20 + 팀별 작업·코칭175 + 마무리15 = 230분)* — **전 회차 중 학생 자율 작업 비중이 가장 높은 회차**
- **누적 공수 ~15h**
- **NCS 능력단위요소** `2001070405_20v1.2` 인공지능서비스 운영품질 점검하기 / `2001070408_20v1.1` 인공지능서비스 운영개선사항 식별하기 / `2001070408_20v1.3` 인공지능서비스 운영개선사항 수행하기 (부분) / `2001070403_20v1.4` 인공지능서비스 운영매뉴얼 관리하기 (부분)

---

### 14주차 — 최종 발표 및 마무리

- **팀별 발표(15분 + 질의 5분)** — 라이브 시연 포함
- **상호평가** 팀 간 평가지 작성
- **리소스 전체 정리 실습(40분)** 인스턴스 종료 · 엔드포인트 삭제 · ECR/S3 정리 · **최종 비용 확인 및 리뷰**
- **회고 및 진로 연계(30분)** 포트폴리오 정리 가이드(GitHub README 작성법), 관련 자격증·다음 학습 경로
- *(시간 배분: 팀당 20분 × 6~7팀 130 + 휴식20 + 리소스 정리40 + 회고·마무리40 = 230분. 팀 수가 8팀 이상이면 발표를 12분으로 단축)*
- **누적 공수 ~18~20h ✅**
- **NCS 능력단위요소** `2001070208_18v1.1` 인공지능 서비스 성과기준 기획하기 (부분) / `2001070208_18v1.2` 인공지능 서비스 성과 평가 방법 기획하기 (부분) / `2001070302_19v1.3` 인공지능 모델 설계 검증하기 (부분) / `2001070405_20v1.3` 인공지능서비스 운영품질 개선하기 (부분)

---

## 10. 팀 프로젝트 설계

### 개요

| 항목 | 내용 |
|---|---|
| 인원 | 3~4인 |
| 기간 | 3주차 착수 ~ 14주차 발표 (12주) |
| 총 공수 | **18~20시간** (수업 내 약 12h + 팀 자율 약 6~8h) |
| 필수 요건 | 데이터 → 학습 → 실험기록 → 컨테이너 → API 서빙 → 배포 → 최소 1개 모니터링 지표 |

### 도메인 3종 (택 1) + 자유주제

**A. 수요 · 가격 예측 (회귀)**
> 예: 따릉이 대여량 예측 / 지역별 전력 수요 / 농산물 도매가 예측 / 상권 매출 예측
> 데이터: 공공데이터포털, 서울열린데이터광장
> 난이도: ★★☆ — 시계열 특성 처리에 주의. 가장 무난한 선택

**B. 이탈 · 불량 · 리스크 분류 (분류)**
> 예: 통신사 고객 이탈 / 제조 공정 불량 판정 / 대출 상환 여부 / 학생 중도탈락 예측
> 데이터: Kaggle 공개 데이터셋
> 난이도: ★☆☆ — 정형 이진분류. 가장 쉬움. 불균형 데이터 처리가 학습 포인트

**C. 텍스트 리뷰 분석 (분류 + 생성형 AI)**
> 예: 상품/영화 리뷰 감성 분류 + Bedrock으로 리뷰 요약 및 개선점 도출
> 데이터: 네이버 영화 리뷰(NSMC), 공개 리뷰 데이터셋
> 난이도: ★★★ — 10주차 Bedrock 실습과 가장 잘 붙음. 흥미도 높음

**D. 자유주제 (사전 승인 필수)**
승인 기준 — 아래 **전부** 충족해야 함:
- [ ] 데이터를 3주차 안에 실제로 확보 가능 (링크 제시)
- [ ] 정답(라벨)이 존재하거나 명확히 정의 가능
- [ ] 개인정보 · 저작권 문제 없음
- [ ] 데이터 크기 500MB 이하
- [ ] CPU로 **10분 이내** 학습 가능 (대형 딥러닝 모델 학습 금지)

> 이미지 딥러닝 주제는 학습 시간·비용 때문에 원칙적으로 비권장. 굳이 한다면 **사전학습 모델 파인튜닝 없이 특징 추출만** 사용하는 조건으로 승인.

### 마일스톤

| 마일스톤 | 시점 | 제출물 | 배점 |
|---|---|---|---|
| **M1** 문제 정의 | 4주차 | 문제정의서(주제·데이터·입출력·평가지표·성공기준) + 데이터 카드 | 10 |
| **M2** 중간 시연 | 8주차 | 베이스라인 모델 + 호출되는 API (5분 시연) | 15 |
| **M3** 파이프라인 완성 | 12주차 | 전 구간 연결 확인 (체크리스트 통과) | 실습 점수 반영 |
| **M4** 최종 발표 | 14주차 | 코드 저장소 + README + 발표 + 라이브 시연 | 25 + 15 |

### 팀 역할 (권장 · 순환)
- **데이터 담당** 수집·전처리·데이터 카드
- **모델 담당** 학습·실험 기록·성능 개선
- **서빙 담당** API·컨테이너·배포
- **운영/문서 담당** 모니터링·README·발표 자료

> 단, **각자 최소 1회는 전 구간을 혼자 돌려보기**를 요구한다 (개인 체크포인트로 확인). 역할 분담이 "나는 그거 몰라요"로 끝나지 않게 하는 장치.

---

## 11. 리스크 및 대응

| 리스크 | 대응 |
|---|---|
| 학교망에서 SSH(22) 차단 | EC2 Instance Connect / Session Manager로 대체 (개강 전 테스트) |
| 학생별 진도 편차 확대 | 회차별 완성본 저장소 태그 제공 → 뒤처진 팀은 완성본에서 이어감 |
| Bedrock 모델 액세스 승인 지연 | 개강 2주 전 강사가 사전 신청·확인 |
| 데이터 확보 실패로 시간 낭비 | 도메인별 **사전 검증된 데이터셋 목록**을 3주차에 배포 |
| 비용 초과 | Budgets 알람 + 인스턴스 타입 제한 IAM 정책 + 매주 정리 타임 |
| 팀 무임승차 | 개인 체크포인트 + 상호평가 + 커밋 기록 확인 |
| 최종 발표 시 데모 실패 | 13주차에 **시연 영상 백업 의무화** |

---

## 12. 강사 사전 준비 체크리스트

**개강 2주 전**
- [ ] 학생별 IAM 계정 발급 및 권한 정책(인스턴스 타입 제한 포함) 적용
- [ ] AWS Budgets 학생별/전체 알람 설정
- [ ] Bedrock 모델 액세스 신청 및 승인 확인 (서울 리전)
- [ ] 학교망에서 SSH·8000·8501 포트 접속 테스트

**개강 1주 전**
- [ ] 공통 예제 데이터셋 확정 및 S3 업로드
- [ ] 회차별 완성본 저장소 + 태그 준비 (`week-01-done` ~ `week-12-done`)
- [ ] 명령어 치트시트 · Dockerfile/YAML 템플릿 제작
- [ ] 도메인별 검증 데이터셋 목록 작성
- [ ] SageMaker/Bedrock 콘솔 화면 캡처 갱신 (UI 변경 잦음)

---

## 13. 다음 단계 (제작 예정 산출물)

0. ~~NCS 매핑~~ ✅ 완료 — `NCS매핑표_v2.md` (5개 세분류 / 능력단위요소 50개 채택)
1. **회차별 웹 교재** (학생 참여형 장표 + 발표자 대본 + 실습 가이드) — 15주
2. **실습 워크북** (복붙 가능한 명령어 · 코드 · 스크린샷 체크포인트)
3. **레퍼런스 구현 저장소** (주차별 태그 포함)
4. **프로젝트 안내서 + 평가 루브릭 배포본** — 채택 능력단위요소의 **수행준거**를 회차별 체크포인트 문항으로 전환
5. **강사용 운영 매뉴얼** (계정 발급 · 비용 관리 · 장애 대응)
